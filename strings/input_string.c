#include<stdio.h>
int main ()  
{
    char str[20];
    scanf("%s",str); // we did not use & in case of taking input a string because string itself a array and name of array itself represnt the base address of the array  
return 0;
}
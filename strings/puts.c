// puts() is a function used to print the string and move  the cursor to next line of the string 
#include<stdio.h>
int main ()  
{
    char str[50];
    gets(str);
    // printf("%s",str);
    puts(str); // same as printf("%s\n",str);
    printf("Hii");
return 0;
}